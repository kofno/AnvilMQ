AnvilMQ is running as ONE broker. There is no replication or automatic broker failover.

gRPC: {{ include "anvilmq.name" . }}.{{ .Release.Namespace }}.svc.cluster.local:50051
Metrics/readiness: http://{{ include "anvilmq.name" . }}.{{ .Release.Namespace }}.svc.cluster.local:9090
Durability: {{ .Values.durability }}

Default NetworkPolicy permits same-namespace pods labelled anvilmq-client=true.
RPCs are plaintext and unauthenticated. Only allow trusted evaluation workloads.
PVCs are retained on uninstall. A single-pod upgrade causes an interruption.
{{- if .Values.dashboards.enabled }}

Grafana dashboards are provisioned via ConfigMap {{ include "anvilmq.name" . }}-dashboards
(label {{ .Values.dashboards.label | default "grafana_dashboard" }}={{ .Values.dashboards.value | default "1" | quote }}).
The Grafana dashboard sidecar must be enabled and watching {{ .Values.dashboards.namespace | default .Release.Namespace }}.
{{- end }}
